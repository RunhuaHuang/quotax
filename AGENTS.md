# AGENTS.md

## 识图规则

对于**非多模态大模型**（不支持原生识图）或**原生识图失败**的情况，识图任务**首选 `visionpower` MCP** 来完成：

- 需要识别图片时，先判断当前模型是否支持原生识图；不支持，或原生识图失败/结果不可用时，立即改用 visionpower MCP 的识图工具（`mcp__visionpower__*`）。
- 以 visionpower 返回的识别结果为准，基于其结果继续完成用户请求；不得臆测图片内容，也不得直接回复「无法识别」。
